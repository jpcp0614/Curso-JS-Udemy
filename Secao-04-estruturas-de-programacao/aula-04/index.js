/*
* Palavras reservadas em JS:

break case catch class const continue debugger default delete do else
enum export extends false finally for function if implements import
interface in instanceof let new package private protected public return
static super switch this throw true try typeof var void while with yield

*/

// let break = 1 // não pode
let breakOne = 1;

// let function = true; // não pode
let functionOne = true;

console.log('breakOne: ', breakOne);
console.log('functionOne: ', functionOne);