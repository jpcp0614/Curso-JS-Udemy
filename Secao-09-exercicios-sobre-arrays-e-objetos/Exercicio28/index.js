const bus = {
  wheel: 8,
  seats: 40,
  doors: 2,
}

console.log(`As propriedades do objeto bus são: ${Object.keys(bus)}`);
console.log(`O valor das propriedades do objeto bus são: ${Object.values(bus)}`);